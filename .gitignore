# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Scratch / generated output
data/benchmark*.log

# Precomputed pattern-matrix caches (regenerate with --build-matrix)
data/patterns.bin
data/patterns_full.bin

# Per-game state written by wordle_next.py --new/--hint (transient)
data/games/

# Harness / tooling artifacts (but keep committed project skills, hooks, config)
.claude/*
!.claude/skills/
!.claude/hooks/
!.claude/settings.json
